

 - uses: yanzay/notify-telegram@v0.1.0
      if: always()
      with:
        chat: ${{ secrets.canal_edd_id }} # user id or channel name secret
        token: ${{ secrets.token_bot_edd }} # token secret
        status: ${{ job.status }} # do not modify this line
